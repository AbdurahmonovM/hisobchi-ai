# ---- Core bot framework ----
aiogram==3.13.1

# ---- Web server for the Telegram Web App + API ----
fastapi==0.115.0
uvicorn[standard]==0.31.0
jinja2==3.1.4              # optional templating (we serve static HTML, kept for convenience)

# ---- Database (async) ----
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0          # async SQLite driver (dev default)
asyncpg==0.30.0            # async PostgreSQL driver (prod; has Python 3.13 wheels)

# ---- AI / LLM ----
# Google Gemini (multimodal): transcribes the voice AND extracts transactions.
# One model handles audio + multi-transaction NLP.
google-genai>=1.0.0

# ---- Settings & validation ----
pydantic==2.9.2
pydantic-settings==2.5.2

# ---- Misc ----
python-dotenv==1.0.1
httpx==0.27.2
